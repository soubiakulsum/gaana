<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project_gaana">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_gaana">
        <activity android:name=".activities.PlaySongMusicActivity"></activity>
        <activity android:name=".activities.PlayHindiMusicActivity" />
        <activity android:name=".activities.HindiDummyActivity" />
        <activity android:name=".activities.GaanaVideoActivity" />
        <activity android:name=".activities.PlayMusicActivity" />
        <activity android:name=".activities.DummyActivity" />
        <activity android:name=".activities.GetOTP"/>
        <activity android:name=".activities.MyHotShots" />
        <activity android:name=".activities.HotshotsActivity" />
        <activity android:name=".activities.SearchSongActivity" />
        <activity android:name=".activities.EnterNumber" />
        <activity android:name=".activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ContinueWithPhoneNumber" />
        <activity android:name=".activities.SettingsActivity" />

        <service
            android:name=".services.Empty"
            android:enabled="true"
            android:exported="true" /> <!-- <activity android:name=".activities.MainActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- </activity> -->
        <!-- <activity android:name=".activities.MainActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- </activity> -->
        <!-- <activity android:name=".activities.MainActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- </activity> -->
        <service
            android:name=".services.Empty"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activities.MainActivity"></activity>
    </application>

</manifest>